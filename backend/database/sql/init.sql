PRAGMA foreign_keys = ON;

/* Apply in this order for a fresh SQLite demo database:
   1. tables.sql
   2. indexes.sql
   3. views.sql
   4. triggers.sql
   5. seed_data.sql

   This file intentionally contains the recommended execution order so it can
   be used as an operator checklist alongside the split SQL assets.
*/

