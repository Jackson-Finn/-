# 前端修改说明

## 修改记录

| 日期 | 版本 | 修改内容 |
|------|------|----------|
| 2026-04-21 | v1.0 | 替换闲置市场 Logo 为自定义图标 |
| 2026-04-21 | v1.0 | 首页文案面向用户语言重新编写 |
| 2026-04-21 | v1.0 | 管理员后台侧边栏"通知运营"与"系统设置"图标补全 |
| 2026-04-21 | v1.0 | 举报/申诉管理页面：表格与详情页状态改为标签显示，已处理条目直接展示结果卡片 |

---

## 修改 1：闲置市场 Logo 替换

### 修改内容

将侧边栏的品牌徽章从文字 "AM"（蓝紫渐变背景）替换为自定义 Logo 图片。

**修改文件：**
- `frontend/public/logo.jpg`（新增，整理至项目）
- `frontend/src/components/AppSidebar.vue`

**具体改动：**

1. 将根目录的 `图标.jpg` 复制到 `frontend/public/logo.jpg`，作为静态资源统一管理
2. `AppSidebar.vue` 中将 `.brand-badge`（文字徽章）替换为 `.brand-logo img`（图片），保留原有尺寸（44×44px）和圆角（14px），移除渐变背景样式

---

## 修改 2：首页文案重写

### 修改背景

原文案面向开发者，强调技术实现（如"围绕发现、沟通、下单与履约组织页面"），不符合面向客户的展示页面风格。

### 修改文件

- `frontend/src/layouts/UserLayout.vue`（顶部 bar 描述）
- `frontend/src/views/user/Home.vue`（首页 Hero、商品大厅、推荐流、交易信号等多个区域）

### 具体改动

| 位置 | 原文案（面向开发者） | 新文案（面向用户） |
|------|----------------------|-------------------|
| 顶部 bar 描述 | 围绕发现、沟通、下单与履约组织页面，把复杂功能收进更清晰的流转里。 | 找好物、聊细节、放心买，轻松完成每一笔二手交易。 |
| Hero 标题 | 更少按钮，更完整的交易路径。 | 发现宝贝，聊好价格，轻松交易。 |
| Hero 描述 | 从搜索开始，快速进入商品、卖家沟通和订单履约，不再把关键动作散落在各个页面里。 | 从搜索到下单，完整交易路径一目了然，随时了解商品动态。 |
| Hero 搜索提示 | 先在商品大厅里逛，再通过搜索快速缩小范围，会更接近常见二手交易平台的使用习惯。 | 在商品大厅里逛逛，或直接搜索缩小范围。 |
| 商品大厅描述 | 先看公开在售商品，再决定要不要收藏、联系卖家或继续搜索。 | 公开在售的好物，看看有没有你想要的。 |
| 推荐流描述 | 理由会直接展示出来，帮助你更快判断为什么值得看。 | 根据你的偏好精选，帮你更快找到心仪好物。 |
| 交易信号区域描述 | 把消息、通知、收藏和最近浏览收在一处，减少反复找入口。 | 消息、订单、收藏和浏览记录统一汇总，随时把握交易进度。 |
| 未读消息卡片 | 直接进入会话，继续处理正在沟通的商品。 | 随时查看正在沟通中的商品消息。 |
| 进行中订单卡片 | 确认收货、补评价和查看履约进度都从这里处理。 | 查看进行中订单的进度和详情。 |
| 收藏夹卡片 | 回到候选商品继续比较，不必重新搜索。 | 从收藏夹快速回到候选商品，继续比较。 |
| 最近浏览卡片 | 把最近看过的商品线索重新收回来。 | 快速回到最近浏览过的商品。 |

---

## 修改 3：管理员后台侧边栏图标补全

### 问题描述

管理员后台左侧导航栏中，"通知运营"和"系统设置"两项图标显示为空，其他三项正常。

### 原因

`frontend/src/main.js` 中的图标注册表 `iconRegistry` 缺少 `Bell`（通知运营）和 `Setting`（系统设置）两个图标组件。

### 修改文件

- `frontend/src/main.js`

### 具体改动

1. 从 `@element-plus/icons-vue` 补充导入 `Bell` 和 `Setting`
2. 在 `iconRegistry` 对象中注册这两个图标

修改后，管理员侧边栏五项菜单均有正常图标显示：

| 菜单项 | 图标 |
|--------|------|
| 审核中心 | DocumentChecked |
| 交易治理 | Warning |
| 通知运营 | Bell |
| 系统设置 | Setting |
| 权限管理 | Lock |

---

## 修改 4：举报/申诉管理页面状态提示优化

### 问题描述

已处理的举报或已复核的申诉，在表格中直接显示原始英文状态码（PROCESSED / APPROVED 等），用户点到详情页才发现操作按钮是禁用的，体验不好。

### 修改文件

- `frontend/src/views/admin/ReportManage.vue`
- `frontend/src/views/admin/AppealManage.vue`

### 具体改动

**举报管理页：**

1. 表格"状态"列：PROCESSED → 绿色"已处理"标签，其余 → 橙色"待处理"标签
2. 详情工作台：已处理的举报不再展示备注表单和禁用按钮，改为 `el-result` 结果卡片，显示"已处理完成"及处理备注

**申诉管理页：**

1. 表格"状态"列：APPROVED → 绿色"已支持"标签，REJECTED → 红色"已驳回"标签，其余 → 橙色"待复核"标签
2. 详情工作台：已复核的申诉不再展示备注表单和禁用按钮，改为 `el-result` 结果卡片，显示最终裁定结果及复核备注
