# 修改说明

## 修改记录

| 日期 | 版本 | 修改内容 |
|------|------|----------|
| 2026-04-21 | v1.1 | 修复订单取消时未恢复库存的问题 |
| 2026-04-23 | v1.2 | 修复推荐流会推荐未经审核或已下架商品的问题 |

---

## 问题 1：订单取消时未恢复库存

### 问题描述

在 `cancel_order` 方法中，当买家取消订单时，系统仅将订单状态更新为 `CANCELLED`，但并未将下单时扣减的库存返还给商品。这导致商品库存永久丢失，即使订单已取消也无法恢复。

### 问题影响

- 商家取消订单后，商品库存无法恢复
- 可能导致商品库存数据不准确
- 影响商品的可销售数量统计

### 修复方案

**修改文件：**
- `backend/app/services/trade.py`
- `backend/app/repositories/trade.py`

**修改内容：**

1. 在 `TradeRepository` 中新增 `get_order_items` 方法，用于查询订单关联的商品明细：

```python
def get_order_items(self, order_id: int) -> list[OrderItem]:
    return self.db.query(OrderItem).filter_by(order_id=order_id).all()
```

2. 在 `TradeService.cancel_order` 方法中，订单取消前先查询订单商品明细，将扣减的库存加回商品库存：

```python
def cancel_order(self, order_id: int, user_id: int):
    order = self.get_order(order_id, user_id)
    if order.buyer_id != user_id or order.status != OrderStatus.CREATED.value:
        raise AppError("Order cannot be cancelled", status_code=400)
    order.status = OrderStatus.CANCELLED.value

    # 恢复库存
    order_items = self.repo.get_order_items(order_id)
    for item in order_items:
        product = self.catalog_repo.get_product(item.product_id)
        if product:
            product.stock += item.quantity

    self.publisher.publish(...)
    self.db.commit()
    return order
```

### 测试说明

#### 测试环境准备

1. 确保后端服务已启动
2. 准备测试账号（以 seed 数据为例）：
   - 买家账号：`buyer@example.com` / `Buyer123!`
   - 卖家账号：`seller@example.com` / `Seller123!`

#### 测试用例

**前置条件：**
- 获取买家 JWT token（通过登录接口）
- 确定一个库存数量大于 0 的商品（如商品 ID=1，初始库存=1）

**测试步骤：**

1. **创建订单并确认库存扣减**

```bash
curl -X POST http://localhost:8000/api/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <BUYER_TOKEN>" \
  -d '{"product_id": 1, "quantity": 1}'
```

验证：商品库存应从 1 变为 0

2. **取消订单**

```bash
curl -X POST http://localhost:8000/api/orders/<ORDER_ID>/cancel \
  -H "Authorization: Bearer <BUYER_TOKEN>"
```

3. **验证库存已恢复**

```bash
curl http://localhost:8000/api/products/1 \
  -H "Authorization: Bearer <BUYER_TOKEN>"
```

**预期结果：**
- 取消订单后，商品库存应恢复到 1（与下单前一致）
- 订单状态变为 `CANCELLED`

**修复前后对比：**

| 操作 | 修复前库存 | 修复后库存 |
|------|-----------|-----------|
| 下单前 | 1 | 1 |
| 下单后 | 0 | 0 |
| 取消订单后 | 0（未恢复） | 1（已恢复） |

### 验证方法

可以通过数据库直接查询验证：

```sql
-- 查询商品库存
SELECT id, title, stock FROM products WHERE id = 1;

-- 查询订单状态
SELECT id, status FROM orders WHERE product_id = 1 ORDER BY created_at DESC;
```

---

## 问题 2：推荐流会推荐未经审核或已下架的商品

### 问题描述

推荐系统（首页推荐 `/api/recommendations/home` 和相关商品推荐 `/api/recommendations/products/{id}/related`）未对商品的审核状态和销售状态进行过滤，导致可能向用户推荐以下类型的商品：
- `audit_status` 为 `PENDING`（待审核）或 `CHANGES_REQUESTED`（需修改）的商品
- `product_status` 为 `OFF_SHELF`（已下架）的商品

这些商品不应出现在面向用户的推荐流中。

### 问题影响

- 用户可能看到未通过审核的商品，影响平台信任度
- 已下架商品仍出现在推荐中，用户点击后无法购买，体验差
- 快照中缓存的推荐结果在商品后续被下架后不会自动失效

### 问题定位

**涉及文件：**

1. `backend/app/repositories/trade.py` — `popular_products` 方法
2. `backend/app/services/intelligence.py` — `recommend_related` 方法、`_hydrate_recommendation_items` 方法

### 修复方案

**1. 修复 `popular_products`（数据库层过滤）**

`backend/app/repositories/trade.py` 中 `popular_products` 方法原实现：

```python
def popular_products(self, limit: int = 10) -> list[Product]:
    return self.db.query(Product).order_by(Product.created_at.desc()).limit(limit).all()
```

修复后：

```python
def popular_products(self, limit: int = 10) -> list[Product]:
    return (
        self.db.query(Product)
        .filter(
            Product.audit_status == AuditStatus.APPROVED.value,
            Product.product_status == ProductStatus.ACTIVE.value,
        )
        .order_by(Product.created_at.desc())
        .limit(limit)
        .all()
    )
```

**2. 修复 `recommend_related`（内存层过滤）**

`backend/app/services/intelligence.py` 中 `recommend_related` 方法原实现：

```python
def recommend_related(self, product_id: int):
    product = self.catalog_repo.get_product(product_id)
    related = [
        candidate
        for candidate in self.catalog_repo.list_products()
        if candidate.id != product_id and candidate.product_status == "ACTIVE"
    ][:6]
```

修复后增加了 `audit_status` 过滤：

```python
def recommend_related(self, product_id: int):
    product = self.catalog_repo.get_product(product_id)
    related = [
        candidate
        for candidate in self.catalog_repo.list_products()
        if candidate.id != product_id
        and candidate.audit_status == "APPROVED"
        and candidate.product_status == "ACTIVE"
    ][:6]
```

**3. 修复 `_hydrate_recommendation_items`（快照回源时过滤）**

该方法在从推荐快照加载结果时对每个商品进行 JSON → Card 转换，原实现未校验商品当前状态，修复后增加了状态检查：

```python
def _hydrate_recommendation_items(self, raw_items: list[dict]) -> list[dict]:
    items: list[dict] = []
    for item in raw_items:
        product_id = item.get("product_id") or item.get("id")
        product = self.catalog_repo.get_product(product_id) if product_id else None
        # 新增状态过滤：仅展示已审核通过且上架中的商品
        if product and product.audit_status == "APPROVED" and product.product_status == "ACTIVE":
            payload = self._product_card(product)
            payload["reason"] = item.get("reason", "Behavior-based recommendation")
            items.append(payload)
    return items
```

### 测试说明

#### 测试环境准备

1. 确保后端服务已启动
2. 准备测试账号（以 seed 数据为例）：
   - 管理员账号：`admin@example.com` / `Admin123!`
   - 卖家账号：`seller@example.com` / `Seller123!`

#### 测试用例

**用例 1：首页推荐不包含待审核商品**

**前置条件：**
- 获取卖家 JWT token
- 发布一个新品（audit_status=PENDING 状态）

**测试步骤：**

```bash
# 1. 卖家发布商品（状态为 PENDING）
curl -X POST http://localhost:8000/api/products \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <SELLER_TOKEN>" \
  -d '{"title": "测试商品", "description": "测试描述", "price": 100, "stock": 1, "category_id": 1}'

# 2. 验证首页推荐中不包含该商品
curl http://localhost:8000/api/recommendations/home
```

**预期结果：**
- 首页推荐返回的商品列表中，`audit_status` 全部为 `APPROVED`，`product_status` 全部为 `ACTIVE`

---

**用例 2：相关商品推荐不包含未通过审核商品**

**前置条件：**
- 有一个已审核通过的商品（ID=1）和一个待审核商品（ID=2）

**测试步骤：**

```bash
# 查询 ID=1 的相关商品
curl http://localhost:8000/api/recommendations/products/1/related
```

**预期结果：**
- 返回的相关商品中，`audit_status` 全部为 `APPROVED`，`product_status` 全部为 `ACTIVE`
- 待审核商品（ID=2）不会出现在推荐列表中

---

**用例 3：管理员下架商品后，推荐流自动排除**

**前置条件：**
- 有一个已审核通过且上架中的商品
- 该商品已被加入推荐快照

**测试步骤：**

```bash
# 1. 获取管理员 token
# 2. 管理员强制下架商品
curl -X POST http://localhost:8000/api/admin/products/<PRODUCT_ID>/off-shelf \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <ADMIN_TOKEN>" \
  -d '{"note": "违规下架"}'

# 3. 验证首页推荐中该商品不再出现
curl http://localhost:8000/api/recommendations/home
```

**预期结果：**
- 已下架商品（product_status=OFF_SHELF）不会出现在任何推荐接口中

---

## 后续待修复问题

1. **WebSocket 认证缺失** - websocket 端点未验证 JWT token
2. **注册密码强度未校验** - 没有验证用户密码强度
3. **Login.vue 硬编码默认凭据** - 应从配置读取演示账号
4. **JWT secret_key 未校验** - 生产环境不应使用默认密钥
